sudo apt update && apt upgrade -y
sudo apt install ./Yandex.deb
sudo apt install git
ssh -V
ssh-keygen -t ed25519 -C "your_email@example.com"
/root/.ssh - папка скрыта по умолчанию
sudo ssh -T git@github.com       -> yes
sudo apt install composer
sudo apt install apache2

Если index.php читается как текст
sudo apt update && sudo apt install php libapache2-mod-php

sudo apt install snapd -y
sudo snap install postman

включить apache_mod_rewrite, для единой точки входа
sudo a2enmod rewrite

Ввести текстовую команду sudo apt install xrdp и нажать «Ввод».
Провести активацию программы командой sudo systemctl enable xrdp.

sudo apt install postgresql


Смена владельца папки решила мою проблему:

sudo chown -R system-name project-folder-location

/var/www/cloud_storage/.git/config
[remote "origin"]
	url = git@github.com:AnufrienokAlexey/cloud_storage.git
